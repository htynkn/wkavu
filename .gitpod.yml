tasks:
  - init: cargo install cargo-watch && cargo build
    command: cargo build

vscode:
  extensions:
    - k--kato.intellij-idea-keybindings
    - GitHub.copilot
    - rust-lang.rust-analyzer
    - vadimcn.vscode-lldb